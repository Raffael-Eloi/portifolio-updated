Delegate to the `devops-advisor` agent.

If $ARGUMENTS is provided, pass it to the agent as the specific subject or question to focus on.

The agent will review staged and unstaged infrastructure changes and return advisory guidance — no files will be modified.