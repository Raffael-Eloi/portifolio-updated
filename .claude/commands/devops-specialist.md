Delegate to the `devops-specialist` agent.

If $ARGUMENTS is provided, pass it to the agent as the specific subject, file path, or area to focus on (e.g. "security", "cost", "infrastructure/main.tf").

The agent will produce a structured findings report and save it to `docs/infrastructure-review-<date>.md`.